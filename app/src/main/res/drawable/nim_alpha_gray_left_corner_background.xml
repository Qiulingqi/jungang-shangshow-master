<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 边框圆角矩形 -->
    <gradient
        android:endColor="@color/gift_background_color"
        android:startColor="@color/gift_background_color" />

    <solid android:color="@color/gift_background_color" />

    <stroke
        android:width="2px"
        android:color="@color/gift_background_color" />

    <corners
        android:bottomLeftRadius="30dp"
        android:bottomRightRadius="10dp"
        android:topLeftRadius="30dp"
        android:topRightRadius="10dp" />

</shape>